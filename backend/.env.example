PORT=4000

DATABASE_URL=postgresql://claims_engine:changeme@localhost:5432/claims_engine

# "anthropic" for a real LLM call, "mock" for a deterministic offline stand-in
# behind the same LlmProvider interface (see src/llm/llmProvider.js).
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=your-key-here
ANTHROPIC_MODEL=claude-sonnet-5
